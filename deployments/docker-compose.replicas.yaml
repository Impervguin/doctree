services:
  doctree-postgres-replica:
    container_name: doctree-postgres-replica
    hostname: pg-replica
    image: postgres:17.4-alpine3.20
    env_file:
      - pg-replica.env
    ports:
      - 5433:5432
    volumes:
      - doctree-postgres-replica-data:/var/lib/postgresql/data
      - ./setup-master.sh:/usr/local/bin/setup-master.sh
      - ./setup-subscription.sh:/usr/local/bin/setup-subscription.sh
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 4G
    networks:
      - doctree-postgres-net
    entrypoint: >
      sh -c "
        exec docker-entrypoint.sh postgres -c 'max_connections=50' -c 'shared_buffers=1GB' -c 'effective_cache_size=4GB' -c 'work_mem=16MB' -c 'maintenance_work_mem=512MB' -c 'wal_level=logical' -c 'max_wal_senders=10' -c 'max_replication_slots=10' -c 'listen_addresses=*' &
        
        POSTGRES_PID=$!
        # Wait for PostgreSQL to start
        until pg_isready -U $POSTGRES_USER -d $POSTGRES_DB; do
          echo 'Waiting for PostgreSQL to start...'
          sleep 2
        done
        
        # Some additional time for initialization
        sleep 5
        
        echo 'PostgreSQL is ready. Setting up replication...'
        
        # Run setup scripts
        if [ -f /var/lib/postgresql/data/postgresql.conf ]; then
          echo 'Setting up master replication...'
          /usr/local/bin/setup-master.sh
          
          echo 'Setting up subscription...'
          /usr/local/bin/setup-subscription.sh
        fi
        
        wait $POSTGRES_PID
      "
      
  doctree-postgres-migrator-replica:
    container_name: doctree-postgres-migrator-replica
    hostname: pg-migrator-replica
    build:
      context: ..
      dockerfile: pkg/pgmigrator/Dockerfile
    environment:
      - GOOSE_MIGRATION_DIR=/migrations
    env_file:
      - goose.replica.env
    command:
      - "goose"
      - "up"
    volumes:
      - ../migrations:/migrations
    depends_on:
      doctree-postgres-replica:
        condition: service_healthy
    networks:
      - doctree-postgres-net

  doctree-auth-replica:
    container_name: doctree-auth-replica
    hostname: auth-replica
    build:
      context: ..
      dockerfile: ./services/auth-service/Dockerfile
    env_file:
      - ../config/auth.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/auth.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-auth-replica-readonly1:
    container_name: doctree-auth-replica-readonly1
    hostname: auth-replica-readonly1
    build:
      context: ..
      dockerfile: ./services/auth-service/Dockerfile
    env_file:
      - ../config/auth.readonly1.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/auth.readonly1.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-auth-replica-readonly2:
    container_name: doctree-auth-replica-readonly2
    hostname: auth-replica-readonly2
    build:
      context: ..
      dockerfile: ./services/auth-service/Dockerfile
    env_file:
      - ../config/auth.readonly2.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/auth.readonly2.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-tree-replica:
    container_name: doctree-tree-replica
    hostname: tree-replica
    build:
      context: ..
      dockerfile: ./services/tree-service/Dockerfile
    env_file:
      - ../config/tree.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/tree.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-tree-replica-readonly1:
    container_name: doctree-tree-replica-readonly1
    hostname: tree-replica-readonly1
    build:
      context: ..
      dockerfile: ./services/tree-service/Dockerfile
    env_file:
      - ../config/tree.readonly1.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/tree.readonly1.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-tree-replica-readonly2:
    container_name: doctree-tree-replica-readonly2
    hostname: tree-replica-readonly2
    build:
      context: ..
      dockerfile: ./services/tree-service/Dockerfile
    env_file:
      - ../config/tree.readonly2.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/tree.readonly2.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-files-replica:
    container_name: doctree-files-replica
    hostname: files-replica
    build:
      context: ..
      dockerfile: ./services/files-service/Dockerfile
    env_file:
      - ../config/files.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/files.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-files-replica-readonly1:
    container_name: doctree-files-replica-readonly1
    hostname: files-replica-readonly1
    build:
      context: ..
      dockerfile: ./services/files-service/Dockerfile
    env_file:
      - ../config/files.readonly1.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/files.readonly1.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-files-replica-readonly2:
    container_name: doctree-files-replica-readonly2
    hostname: files-replica-readonly2
    build:
      context: ..
      dockerfile: ./services/files-service/Dockerfile
    env_file:
      - ../config/files.readonly2.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/files.readonly2.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-documents-replica:
    container_name: doctree-documents-replica
    hostname: documents-replica
    build:
      context: ..
      dockerfile: ./services/documents-service/Dockerfile
    env_file:
      - ../config/documents.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/documents.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-documents-replica-readonly1:
    container_name: doctree-documents-replica-readonly1
    hostname: documents-replica-readonly1
    build:
      context: ..
      dockerfile: ./services/documents-service/Dockerfile
    env_file:
      - ../config/documents.readonly1.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/documents.readonly1.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
  
  doctree-documents-replica-readonly2:
    container_name: doctree-documents-replica-readonly2
    hostname: documents-replica-readonly2
    build:
      context: ..
      dockerfile: ./services/documents-service/Dockerfile
    env_file:
      - ../config/documents.readonly2.replica.env
    depends_on:
      - doctree-postgres-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/documents.readonly2.replica.env:/usr/src/app/.env
      - ../services/protos:/usr/src/protos
    
  doctree-gateway-replica:
    container_name: doctree-gateway-replica
    hostname: gateway-replica
    build:
      context: ..
      dockerfile: ./services/api-gateway-service/Dockerfile
    env_file:
      - ../config/gateway.replica.env
    depends_on:
      - doctree-postgres-replica
      - doctree-tree-replica
      - doctree-files-replica
      - doctree-documents-replica
      - doctree-auth-replica
    networks:
      - doctree-postgres-net
      - doctree-minio-net
    volumes:
      - ../config/gateway.replica.env:/usr/src/app/.env
      - ../config/services.replica.json:/usr/src/app/config/services.json

volumes:
  doctree-postgres-replica-data:

networks:
  doctree-postgres-net:
    external: true
  doctree-minio-net:
    external: true
  doctree-traefik-net:
    external: true